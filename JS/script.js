let promo_items = document.querySelectorAll('.promo-slider__item');
console.log(promo_items);

let promo_buttons = document.querySelectorAll('.promo-slider__input')
console.log(promo_buttons);


for (i = 0; i < promo_buttons.length; i++) {
  promo_buttons[i].oncklick = function slider(e) {

  }
}